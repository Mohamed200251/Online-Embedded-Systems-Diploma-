/*
 * main.c
 *
 *  Created on: Jul 6, 2023
 *      Author: Alkhouly
 */
#include <stdio.h>
#include <string.h>
int main()
{
	char a[100];
	int i=0;
	printf("Enter a string: ");
	fflush(stdin); fflush(stdout);
	gets(a);
	while (a[i]!=0)
		i++;
	printf("Length of string = %d",i);
}
